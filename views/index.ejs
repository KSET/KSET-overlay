<!DOCTYPE html>
<html>
    <head>
        <% if(locals.styles) { %>
            <% styles.forEach(({ link, tag }) => { -%>
                <link rel="stylesheet" href="<%- `${link}?v=${tag}` -%>">
            <% }) -%>
        <% } %>
    </head>
    <body>
        <div class="meta-data"><%= `${rateLimiter.left}/${rateLimiter.max}` -%></div>
        <form method="POST" id="chat-message-form">
            <input <%- rateLimiter.ready || 'disabled="disabled"' -%> name="text" type="text" id="chat-message-input">
        </form>
        <% if(locals.scripts) { -%>
            <% scripts.forEach(({ link, href, tag }) => { -%>
                <script src="<%- `${link}?v=${tag}` -%>"></script>
            <% }) -%>
        <% } -%>
    </body>
</html>
